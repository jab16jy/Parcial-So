# OpenSpec Configuration — SysAdmin Assistant
# Auto-generated by sdd-init on 2026-06-13

project: SysAdmin Assistant
description: >
  Python CLI app for OS automation tasks. Provides system monitoring,
  process management, and scheduled maintenance operations.
stack:
  language: Python 3
  framework: none
  key_libraries:
    - psutil (system/proces info)
    - rich (CLI output/formatting)
    - schedule (cron-like task scheduling)
  test_runner: none
  strict_tdd: false
  type_checker: none
  linter: none
  formatter: none

persistence:
  mode: openspec
  artifact_store: file

testing:
  strict_tdd: false
  rationale: >
    No test runner detected. This is a scripting/automation project without
    pytest, unittest, or any test framework. Strict TDD would require manual
    test scaffolding that exceeds the project scope.
  capabilities:
    unit: false
    integration: false
    e2e: false
  coverage: false
  quality:
    linter: false
    type_checker: false
    formatter: false

phase_rules:
  explore:
    inputs: [user request, idea, problem]
    outputs: [proposal]
  proposal:
    inputs: [exploration output]
    outputs: [spec]
  spec:
    inputs: [proposal]
    outputs: [design]
  design:
    inputs: [spec]
    outputs: [tasks]
  tasks:
    inputs: [design]
    outputs: [apply-ready plan]
  apply:
    inputs: [tasks]
    outputs: [implemented code]
  verify:
    inputs: [apply output, spec]
    outputs: [verification report]
  archive:
    inputs: [verified change]
    outputs: [delta spec in archive/]
